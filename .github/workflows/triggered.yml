name: Child Workflow

on:
  workflow_dispatch:
    inputs:
      imageTag:
        type: string
        required: true
        description: Tag of the image for updating

jobs:
  trigger_workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo name
        shell: bash
        run: |
          ls
